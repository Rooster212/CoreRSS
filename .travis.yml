language: csharp
solution: CoreRSS.sln
dist: trusty
sudo: required

mono: none

dotnet: 1.0.1

script: dotnet restore

jobs:
  include:
    - stage: test
    - script:
      - cd ./CoreRSS.Tests
      - dotnet build
      - dotnet test